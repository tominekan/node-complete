/**
 * Useful types that I should be using over multiple files
 */

export type Product = {
    name: string,
    price: number,
    description: string,
    image: number
};

export type Store = Array<Product>;